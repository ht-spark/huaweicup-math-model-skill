# 模型假设

## 职责

说明全文共享、且会影响模型适用范围的前提，让读者能够判断模型在哪些条件下成立。

## 写作顺序

1. 只保留建模、数据口径或求解所必需的假设。
2. 对每项假设说明其来自题意、数据条件、领域常识还是计算可行性。
3. 说明强假设可能带来的限制，并在后续检验或模型评价中回应。

## 边界

不罗列无关常识，不为了简化求解而加入没有依据的假设，也不把局部小问的特殊条件误写成全文前提。