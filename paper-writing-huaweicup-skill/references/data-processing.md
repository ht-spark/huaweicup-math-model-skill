# 数据处理

## 职责

说明从原始数据到可用于模型的分析数据之间发生了什么，并保证处理规则可追溯。

## 写作顺序

1. 说明本节使用的数据文件、字段、样本范围和数据粒度。
2. 描述缺失、重复、异常、时间对齐、单位换算或数据拼接的处理规则。
3. 说明特征构造、指标体系、标准化或编码的原因及对模型的作用。
4. 报告处理后的数据规模或关键统计特征；必要时用图表支持。

## 边界

不得静默删除数据；若处理规则被多个小问共用，可前置集中写，否则放在对应小问内。