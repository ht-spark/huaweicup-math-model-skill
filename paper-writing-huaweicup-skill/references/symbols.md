# 符号说明

## 职责

统一全文复用的变量、参数、集合、缩写和单位，使模型表达可独立阅读且前后一致。

## 写作顺序

1. 优先列出跨多个小问共用的符号。
2. 对每个符号给出名称、含义、单位或取值范围。
3. 局部符号在对应小问首次出现处定义，避免总表膨胀。
4. 与 `modeling_output/说明文档/结果说明.md` 和 `建模证据清单.yaml` 保持同一命名。

## 边界

不混用同一符号表示不同概念；不遗漏单位；不把代码变量名直接当作论文符号。